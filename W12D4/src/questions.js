// nodes refer to html elements in javascript? DOM is a tree structure that represent the HTML of a website,
// and every HTML element is a node. Node is an interface that is implemented by multiple objects,
// including document and element.
// DOM document object model ;

//jquery used to shorten code required to perform actions for example, resizing a picture when a user clicks

// DOM api allows developers to interact with an HTML (or other?) document in order to perform actions on elements.

// what is innerHTML? allows us to change the text between the opening and closing tags
// <div> </div>


//questions; is there a way to check if something is a css element